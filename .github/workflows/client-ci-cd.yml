# name: Frontend CI/CD

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'client/**'
#       - 'docker-compose.yml'
#   workflow_dispatch:

# jobs:
#   # ─── ÉTAPE 1 : Tests (optionnel) ───────────────────────────────────────────
#   test:
#     name: Tests
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#           cache: 'npm'
#           cache-dependency-path: client/package-lock.json

#       - name: Install dependencies
#         working-directory: ./client
#         run: npm ci

#       - name: Check TypeScript
#         working-directory: ./client
#         run: npx tsc --noEmit

#       - name: Lint
#         working-directory: ./client
#         run: npm run lint --if-present

#   # ─── ÉTAPE 2 : Deploy ──────────────────────────────────────────────────────
#   deploy:
#     name: Deploy
#     runs-on: ubuntu-latest
#     needs: test
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Copy source to VPS
#         uses: appleboy/scp-action@v0.1.3
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           port: 22
#           source: "client/, docker-compose.yml"
#           target: "/home/ubuntu/deploiment/hms/"
#           strip_components: 0

#       - name: Rebuild & restart Docker
#         uses: appleboy/ssh-action@v0.1.5
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           port: 22
#           script: |
#             cd /home/ubuntu/deploiment/mhs/

#             echo "→ Arrêt des containers..."
#             docker-compose down

#             echo "→ Rebuild de l'image..."
#             docker-compose build --no-cache

#             echo "→ Démarrage..."
#             docker-compose up -d

#             echo "→ Nettoyage des images inutilisées..."
#             docker image prune -f

#             echo "→ Reload Nginx..."
#             sudo systemctl reload nginx

#             echo "✅ Déploiement terminé !"
#             docker ps