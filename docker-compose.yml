version: '3.8'

services:
  client:
    build: 
      context: ./client/
    container_name: hms_client
    ports:
      - "4200:80"
    restart: always
    # volumes:
    #   - /etc/letsencrypt:/etc/letsencrypt